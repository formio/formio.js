export = Url;
declare const Url_base: {
    new (component: any, settings: any, config: any): import("./Rule");
};
declare class Url extends Url_base {
    constructor(component: any, settings: any, config: any);
    defaultMessage: string;
}
