{% ctx.component.components.forEach(function(tab, index) { %}
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title">{{ ctx.t(tab.label) }}</h4>
    </div>
    <div class="panel-body">
      {{ ctx.tabComponents[index] }}
    </div>
  </div>
{% }) %}
